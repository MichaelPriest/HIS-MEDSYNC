import Link from "next/link";
import { Fingerprint, Stethoscope, UsersRound } from "lucide-react";
import { CadastrosWorkspaceNav, CadastroKpi } from "@/components/cadastros/cadastros-workspace-nav";
import { ListToolbar, Pagination } from "@/components/cadastros/list-toolbar";
import { RecordDrawer } from "@/components/cadastros/record-drawer";
import { SectionPage } from "@/components/painel/section-page";
import { StatusToast } from "@/components/painel/status-toast";
import { createClient } from "@/lib/supabase/server";
import { criarUrlFotoAssinada } from "@/modules/cadastros/fotos";

const PAGE_SIZE = 20;

export default async function PacientesPage({ searchParams }: { searchParams: Promise<{ sucesso?: string; q?: string; page?: string }> }) {
  const { sucesso, q: rawQuery, page: rawPage } = await searchParams;const query=rawQuery?.trim().slice(0,80)||"";const safeQuery=query.replace(/[,%()]/g," ").trim();const queryDigits=query.replace(/\D/g,"");const page=Math.max(1,Number.parseInt(rawPage||"1",10)||1);const from=(page-1)*PAGE_SIZE,to=from+PAGE_SIZE-1;
  const supabase=await createClient();let request=supabase.from("pacientes").select("id,nome_completo,nome_social,cpf,cns,data_nascimento,telefone,email,sexo,cidade,uf,ativo,foto_path,numero_registro,ra",{count:"exact"}).eq("ativo",true);if(safeQuery){const filters=[`nome_completo.ilike.%${safeQuery}%`,`ra.ilike.%${safeQuery}%`];if(queryDigits){filters.push(`cpf.ilike.%${queryDigits}%`,`cns.ilike.%${queryDigits}%`);}if(/^\d+$/.test(queryDigits)&&queryDigits.length<=12)filters.push(`numero_registro.eq.${Number(queryDigits)}`);request=request.or(filters.join(","));}
  const {data:pacientes,error,count}=await request.order("nome_completo").range(from,to);const totalPages=Math.max(1,Math.ceil((count??0)/PAGE_SIZE));const pacientesComFoto=await Promise.all((pacientes??[]).map(async paciente=>({...paciente,foto_url:await criarUrlFotoAssinada(supabase,paciente.foto_path)})));const semCpf=pacientesComFoto.filter(p=>!p.cpf).length,semCns=pacientesComFoto.filter(p=>!p.cns).length;
  return <SectionPage eyebrow="Cadastros / Pacientes" title="Pacientes" description="Identificação única do paciente, com RA/registro, documentos, foto e acesso rápido ao contexto assistencial." primaryActionLabel="Novo paciente" primaryActionHref="/pacientes/novo"><CadastrosWorkspaceNav active="/pacientes"/><StatusToast success={sucesso==="cadastrado"?"Paciente cadastrado com sucesso.":null}/><section className="mb-5 grid gap-3 sm:grid-cols-3"><CadastroKpi label="Pacientes ativos" value={count??0}/><CadastroKpi label="Sem CPF nesta página" value={semCpf}/><CadastroKpi label="Sem CNS nesta página" value={semCns} detail={semCns?"Revisar quando aplicável ao atendimento":"Cadastros exibidos completos"}/></section><section className="ui-card overflow-hidden"><div className="space-y-4 border-b border-slate-100 p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-brand-50 text-brand-700"><UsersRound className="size-5"/></span><div><h2 className="font-semibold text-slate-900">Base de pacientes</h2><p className="text-sm text-slate-500">Localize por nome, CPF, CNS, RA ou número de registro.</p></div></div><span className="w-fit rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">{count??0} registros</span></div><ListToolbar query={query} placeholder="Buscar por nome, CPF, CNS, RA ou registro..."/></div>{error?<p className="p-6 text-sm text-red-700">Não foi possível consultar pacientes. Confirme a migration e as permissões.</p>:pacientesComFoto.length?<div className="divide-y divide-slate-100">{pacientesComFoto.map(paciente=><article key={paciente.id} className="p-5 transition hover:bg-slate-50/70"><div className="flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between"><div className="flex min-w-0 items-center gap-4"><span className="grid size-12 shrink-0 place-items-center overflow-hidden rounded-xl bg-brand-50 text-sm font-semibold text-brand-700" style={paciente.foto_url?{backgroundImage:`url(${paciente.foto_url})`,backgroundPosition:"center",backgroundSize:"cover"}:undefined}>{paciente.foto_url?null:(paciente.nome_social||paciente.nome_completo).slice(0,1).toUpperCase()}</span><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="truncate font-bold text-slate-950">{paciente.nome_social||paciente.nome_completo}</h3><span className="rounded-full bg-brand-50 px-2.5 py-1 text-xs font-bold text-brand-700">{paciente.ra}</span></div><p className="mt-1 text-sm text-slate-500">Registro #{paciente.numero_registro} · Nasc. {new Date(`${paciente.data_nascimento}T12:00:00`).toLocaleDateString("pt-BR")}</p><p className="mt-1 text-xs text-slate-400">CPF {paciente.cpf||"—"} · CNS {paciente.cns||"—"} · {paciente.telefone||"Sem telefone"}</p></div></div><div className="flex flex-wrap items-center gap-2"><Link href={`/pacientes/${paciente.id}/identificacao`} className="ui-button-secondary"><Fingerprint className="size-4"/>Identificação</Link><Link href={`/prontuario?paciente=${paciente.id}`} className="ui-button-secondary"><Stethoscope className="size-4"/>Prontuário</Link><RecordDrawer title={paciente.nome_social||paciente.nome_completo} subtitle={`${paciente.ra} · Registro #${paciente.numero_registro}`} photoUrl={paciente.foto_url} fields={[{label:"Registro",value:String(paciente.numero_registro)},{label:"RA",value:paciente.ra},{label:"CPF",value:paciente.cpf},{label:"CNS",value:paciente.cns},{label:"Nascimento",value:new Date(`${paciente.data_nascimento}T12:00:00`).toLocaleDateString("pt-BR")},{label:"Telefone",value:paciente.telefone},{label:"E-mail",value:paciente.email}]}/></div></div></article>)}</div>:<div className="p-10 text-center"><h3 className="font-semibold text-slate-900">Nenhum paciente encontrado</h3><p className="mt-1 text-sm text-slate-500">Busque por nome, documento, RA ou registro.</p></div>}<Pagination basePath="/pacientes" page={page} totalPages={totalPages} query={query}/></section></SectionPage>;
}
