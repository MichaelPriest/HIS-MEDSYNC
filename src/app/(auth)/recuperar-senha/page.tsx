import Link from "next/link";
import { requestPasswordReset } from "@/modules/auth/actions";
export default async function Reset({ searchParams }: { searchParams: Promise<{ enviado?: string }> }) { const { enviado } = await searchParams; return <div className="w-full max-w-sm"><h2 className="text-2xl font-semibold">Recuperar acesso</h2>{enviado ? <p role="status" className="mt-4 rounded-md bg-emerald-50 p-3 text-emerald-900">Se a conta existir, enviaremos as instruções.</p> : <form action={requestPasswordReset} className="mt-6 space-y-4"><label className="block text-sm font-medium">E-mail institucional<input name="email" type="email" required className="mt-1 w-full rounded-md border p-2.5" /></label><button className="w-full rounded-md bg-brand-700 px-4 py-2.5 text-white">Enviar instruções</button></form>}<Link href="/login" className="mt-5 block text-sm text-brand-700 underline">Voltar para entrar</Link></div>; }
