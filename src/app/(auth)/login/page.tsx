import Link from "next/link";
import { login } from "@/modules/auth/actions";
export default async function Login({ searchParams }: { searchParams: Promise<{ erro?: string }> }) { const { erro } = await searchParams; return <div className="w-full max-w-sm"><h2 className="text-2xl font-semibold">Entrar</h2><p className="mt-2 text-sm text-slate-600">Use sua conta institucional.</p>{erro && <p role="alert" className="mt-4 rounded-md bg-red-50 p-3 text-sm text-red-800">Não foi possível autenticar. Revise os dados.</p>}<form action={login} className="mt-6 space-y-4"><label className="block text-sm font-medium">E-mail<input name="email" type="email" autoComplete="username" required className="mt-1 w-full rounded-md border p-2.5" /></label><label className="block text-sm font-medium">Senha<input name="senha" type="password" autoComplete="current-password" minLength={8} required className="mt-1 w-full rounded-md border p-2.5" /></label><button className="w-full rounded-md bg-brand-700 px-4 py-2.5 font-medium text-white hover:bg-brand-600">Entrar</button></form><Link href="/recuperar-senha" className="mt-5 block text-center text-sm text-brand-700 underline">Esqueci minha senha</Link></div>; }
