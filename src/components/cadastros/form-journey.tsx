import { CheckCircle2, CircleDashed } from "lucide-react";

export type FormJourneyStep={label:string;description:string;required?:boolean};
export function FormJourney({title="Jornada do cadastro",steps}:{title?:string;steps:FormJourneyStep[]}){
  return <aside className="mb-5 rounded-2xl border border-slate-200 bg-slate-50/70 p-4 sm:p-5"><div className="flex flex-col gap-4 xl:flex-row xl:items-start"><div className="xl:w-52"><p className="text-xs font-black uppercase tracking-[0.16em] text-slate-400">{title}</p><p className="mt-1 text-sm text-slate-500">Preencha por blocos. Os campos obrigatórios continuam validados ao salvar.</p></div><div className="grid flex-1 gap-2 sm:grid-cols-2 xl:grid-cols-4">{steps.map((step,index)=><div key={`${step.label}-${index}`} className="rounded-xl border border-slate-200 bg-white p-3"><div className="flex items-start gap-2"><span className="mt-0.5 grid size-6 shrink-0 place-items-center rounded-full bg-brand-50 text-brand-700">{step.required?<CheckCircle2 className="size-3.5"/>:<CircleDashed className="size-3.5"/>}</span><div><p className="text-sm font-bold text-slate-800">{index+1}. {step.label}</p><p className="mt-1 text-xs leading-5 text-slate-500">{step.description}</p>{step.required?<span className="mt-2 inline-block rounded-full bg-rose-50 px-2 py-0.5 text-[10px] font-bold uppercase text-rose-700">Obrigatório</span>:null}</div></div></div>)}</div></div></aside>
}
